# H5 静态站点 Nginx 配置片段（标准 Nginx，非宝塔）
# 构建产物目录：/opt/philosophy/h5/（yarn build:h5 → dist-h5/）
# 访问地址：https://<域名>/app/

    location = /app {
        return 301 /app/;
    }

    location /app/ {
        alias /opt/philosophy/h5/;
        index index.html;
    }
